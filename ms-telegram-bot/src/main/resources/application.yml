management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include:
          - health
      base-path: /
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
